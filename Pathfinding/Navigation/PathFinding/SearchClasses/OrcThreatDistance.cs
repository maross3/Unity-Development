using _Dev._Mike.Scripts.Ground.PathFinding.SearchClasses;

/// <summary>
/// was used as a visual example, but does represent possibilities
/// </summary>
public class OrcThreatDistance : AStar
{
    /*
    private Enemy curEnemy;
    private void Start()
    {
        curEnemy = GetComponent<Enemy>();
    }
    protected override void DoSearch(DevNode curNode)
    {
        if (curNode.priority < 25) NotWorthMyTime(Ranged);
        else if (curNode.priority < 15) NotWorthMyTime(Run);
        else base.DoSearch(curNode);
    }
    */
    
}
